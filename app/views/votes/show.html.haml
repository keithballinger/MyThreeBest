#three-best
  = render "shared/three_best"
%br
%p
  = image_tag @user.profile_picture
%p
  = "#{@user.full_name} votes for #{current_user.full_name} (YOU!)"
#photos
  - @voted_photos.each do |photo|
    = image_tag photo.url

